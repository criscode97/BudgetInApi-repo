from django.contrib import admin
from django.urls import path, include
from .views import  UserBudgetAPIView, ExpenseView, IncomeView


urlpatterns = [
    path('income/', IncomeView.as_view(), name='income'),
    path('income/<int:id>/', IncomeView.as_view(), name='income'),
    path('expenses/', ExpenseView.as_view(), name='expense'),
    path('expense/<int:id>/', ExpenseView.as_view(), name='expense'),
    path('user-budget/',  UserBudgetAPIView.as_view(), name='user_expenses')
]













































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































